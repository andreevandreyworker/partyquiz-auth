# partyquiz-auth

Auth microservice (FastAPI) — login/password registration, JWT issuance, guest sessions.

Part of the **PartyQuiz** project. Built and published to `ghcr.io/andreevandreyworker/partyquiz-auth` via CI on push to `main` and on version tags.
